"""Modules implemented against `graph/contract.py`.

Two, for now: one per-note (`oscillator.WavetableOscillator`) and one
once-only (`delay.Delay`), which is #202's "done when". The first module
set proper is #205, and a hosted plugin arrives here as one more entry with
no privileges the others do not have -- that is what the contract is for.
"""
